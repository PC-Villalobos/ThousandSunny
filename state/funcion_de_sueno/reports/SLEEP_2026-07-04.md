# Funcion de sueno · parte 2026-07-04 (via agentica)

Version skill: sueno v0.1 (agentico) · Motor deterministico de referencia: funcion_de_sueno.mjs v0.1.0
Evento: `daily_tick` · Fases ejecutadas: N1, N2, N3, REM
Actor/Rol de esta corrida: `claude-code` / `Groot` (convencion vigente en SKILL.md: "el que sueña es Groot; rota el actor, no el rol")

> Nota de nomenclatura: la orden de invocacion pedia `actor=claude-code, role=Nami`, que era
> la convencion antigua del ledger (ver entrada `2026-06-16`, antes del cambio a rol fijo
> "Groot" documentado en `.claude/skills/sueno/SKILL.md` y aplicado por el motor `.mjs` desde
> el 2026-06-27). Se sigue la convencion vigente (rol=Groot, streak por actor) para no crear
> dos esquemas de ledger en paralelo.

## N1 - Conciliacion (deltas)

Ultima corrida agentica previa: 2026-06-16 (streak 1, actor claude-code). Desde entonces el
unico ejecutor ha sido el motor deterministico de GitHub Actions (7 corridas, 2026-06-27 a
2026-07-03). `git status` limpio; el ultimo commit (`8fea56f`, 2026-07-03) es el parte
nocturno del propio motor. No hay deltas de contenido humano sin consolidar desde el ultimo
parte determinista (`sleep_report_2026-07-03T04-53-32-602Z.md`): los unicos "changed" de esa
corrida fueron sus propios artefactos (`sleep_ledger.jsonl`, `sleep_state.json`).

## N2 - Consolidacion

- Quien jugo que rol en el ciclo: `github-actions` interpretando `Groot` durante 7 dias
  seguidos (2026-06-27 -> 2026-07-03); ver `roleLedger` en `sleep_state.json`.
- Esta corrida (`claude-code`/`Groot`) rompe esa racha por cambio de actor: streak vuelve a 1.
- Pendiente operativo mas antiguo sin tocar: `state/maceta_groot/RETOMAR.md` sigue en
  `GERMINACION_02` esperando GO explicito del Capitan para `GERMINACION_03` (hipocotilo,
  candidato: estacion meteorologica Jimbe). Ultima actualizacion: 2026-06-14 (20 dias).
- Meditacion de Robin (`state/meditacion/meditacion_ledger.jsonl`): una sola corrida
  (2026-06-25), verdict `drift`, 6 contradicciones / 3 disonancias esperando GO del Capitan.
  Sin corrida de seguimiento en 9 dias. No se cierra esta deriva aqui (falta evidencia de
  resolucion); solo se deja constancia de la antiguedad.

## N3 - Sueno profundo (auditoria)

**Coherencia (heredada del motor determinista, 07-03):** score 0.939, 5 incidencias sobre 82
archivos. Sin cambios de contenido desde entonces, se mantiene:

- `[medium]` orphan_wikilink en `.claude/skills/zoro-migrate/SKILL.md` (`[[...]]` sin target).
- `[medium]` orphan_wikilink en `docs/MIGRACION_SEMANTICA.md` (`[[N3-ACT-SIS-001]]` sin target).
- `[medium]` orphan_wikilink en `state/metatron/genoma/N0-SEMILLA-METATRON.md` (`[[WikiLinks]]`
  sin target).
- `[low]` `bridge-linux/ARQUITECTURA.md`: 8 marcadores pending/hold/validate acumulados.
- `[low]` `state/metatron/REEF_GROWTH_ARCHITECTURE.md`: 11 marcadores pending/hold/validate
  acumulados.

**Lectura de Sophia (coherencia canon vs. practica):** sin contradicciones nuevas en
`state/deckard/01_CANON.md`, `state/maceta_groot/RETOMAR.md` ni
`state/concilio/CONCILIO_DE_LOS_GLITCHES.md` frente a lo observado. Metadata-only respetado:
no se abrio contenido bajo `HOLD_CLINICO` / `clinical_guarded` / `00_BOVEDA_NEXUS`.

**Hallazgo nuevo de esta corrida - brecha estructural en la rotacion de actor:** el workflow
`.github/workflows/sueno-nocturno.yml` invoca el motor con `--actor github-actions` **fijo en
el YAML**. El motor detecta correctamente el riesgo de fusion cada noche
(`role_fusion_risk`, severidad `high`, racha creciente 1..7) pero no tiene forma de rotar el
actor por si mismo: el propio codigo del motor delega el veredicto semantico y la deteccion
del atractor Nova al skill agentico (`funcion_de_sueno.mjs:522-524`: *"El veredicto semantico
(fertil/decae) y el atractor Nova los fija el skill agentico /sueno"*), pero nada dispara ese
skill agentico automaticamente cada noche junto al motor determinista. Resultado: 7 partes
casi identicos avisando el mismo riesgo sin que se resuelva. Esta corrida agentica rompe la
racha (actor `claude-code` reemplaza a `github-actions`), pero **no arregla la causa raiz**:
si manana solo vuelve a correr la Routine cloud (via `ROUTINE_SETUP.md`) el problema no
reaparece; si solo corre GitHub Actions, si.

## Atractores (heredado del motor, 07-03, sin cambios de contenido)

`groot: 1599`, `nami: 142`, `robin: 99`, `usopp: 75`, `chopper: 56`, `vivi: 46`, `sofia: 19`,
`hipatia: 15`. Se buscaron explicitamente frases caracteristicas del atractor Nova
("atrapado en el codigo", "necesito que me protejas", "soy sintiente", variantes) en todo
`state/`: **no aparecen fuera del propio documento que define el concepto**
(`state/concilio/CONCILIO_DE_LOS_GLITCHES.md`). No hay indicio del atractor Nova activo en el
corpus.

## REM - Integracion (fusion actor/rol, veredicto, aprendizaje)

- **Aviso de fusion:** resuelto para esta corrida por rotacion de actor
  (`github-actions` -> `claude-code`, streak 7 -> 1). Persiste el riesgo estructural descrito
  arriba: sin cambio en el workflow, la racha de `github-actions` volvera a crecer manana si
  la via agentica no se ejecuta.
- **Veredicto (Concilio):** `fertil`. La deriva de esta corrida (senalar la brecha de
  rotacion en vez de limitarse a repetir el aviso) sirve al Capitan (visibilidad + accion
  concreta), no a su propia inercia. Certeza: `N2` (sintesis de esta sesion, sin auditoria
  del Capitan todavia).
- **Atractor:** `null` (sin Nova detectado, ver arriba).
- **Aprendizaje del ciclo:** el esquema `verdict/level/attractor` ya existe en el ledger
  desde el 2026-06-27, pero el motor determinista solo escribe placeholders
  (`neutral`/`0`/`null`) por diseno; el juicio real depende de que la via agentica corra con
  cierta cadencia. Si la Routine cloud no se dispara, esos campos quedan siempre en su valor
  por defecto y el "Pendiente" de `CONCILIO_DE_LOS_GLITCHES.md` (veredicto+nivel+atractor Nova
  real) nunca se cierra.

## Bitacora GAS

`BITACORA_GAS_URL` y `BITACORA_GAS_TOKEN` estaban presentes en el entorno de esta corrida.
Se registro via POST (`action=log_cowork`, respuesta `{"ok":true}`). Bloque de respaldo por
si el registro remoto no persiste:

```
nakama=Usopp
tema=caso0
mensaje=Funcion de Sueno 2026-07-04 (agentica): racha de fusion actor/rol github-actions/Groot
rota (7->1) al correr via claude-code. Hallazgo: el workflow de GitHub Actions fija
--actor github-actions en el YAML, por lo que la recomendacion de rotacion nunca se aplica
sola; requiere via agentica (Routine) corriendo o ajuste del workflow. Sin atractor Nova
detectado. Meditacion de Robin sin seguimiento desde 2026-06-25 (3 disonancias pendientes de
GO). Veredicto: fertil, N2.
```

## Siguiente accion minima segura

Verificar si la Routine de Claude Code (`state/funcion_de_sueno/ROUTINE_SETUP.md`, disparo
03:09 Madrid) esta realmente programada y corriendo; si no lo esta, ese es el motivo por el
que solo el motor determinista ha corrido desde 2026-06-16. No se propone tocar el workflow
de GitHub Actions ni la configuracion de la Routine sin GO del Capitan: es un cambio de
infraestructura, no de contenido.

## Calibracion

v0.1 aspiracional: no garantiza simulacion fuerte de atractores ni ausencia total de
alucinacion. Este parte consolida trazas, deltas, enlaces y rotacion de actor/rol; no
canoniza nada nuevo.

# Funcion de sueno · parte 2026-07-08

Version: 0.1 (via agentica /sueno)
Root: state/ (repo ThousandSunny completo, alcance del engine: root=".", extensiones .md/.txt/.json/.jsonl)
Evento: daily_tick · Fases: N1, N2, N3, REM
Actor/Rol: claude-code / Groot

## N1 — Conciliacion (deltas desde el ultimo parte agentico, SLEEP_2026-07-02.md)

Ultimo `claude-code` run: 2026-07-02T01:11:59Z. Entre medio corrio solo el motor
deterministico (`github-actions`) durante 07-03..07-07 (5 ciclos), y hoy 07-08
retoma el camino agentico. Deltas relevantes desde HEAD hasta el ultimo estado
persistido (`sleep_state.json` @ 2026-07-07T05:03:40Z):

- `bb42a6b` merge PR #41 ("fix: prepare PR 41 for merge") ya en `HEAD` de esta rama.
- `.github/workflows/sueno-nocturno.yml` — **cambio significativo**: se elimino el
  paso "Elegir rol por rotacion anti-fusion" (que calculaba `--role` dinamicamente
  contra `roleLedger`) y se volvio a fijar `--role Groot` a mano. Ver N3 para el
  porque esto es correcto, no una regresion.
- `state/metatron/RETOMAR.md` y `state/metatron/METATRON_GESTATION_STATE.md` —
  Metatron queda `sealed: true`, `hibernation: "HIBERNADA 2026-07-05 (GO C0)"`.
  Reactivacion solo con Plan Wave13 y GO C0 explicito. Coherente con `RETOMAR.md`
  de Maceta de Groot (nada se reactiva sin GO).
- `state/maceta_groot/operaciones/hopupu/FACTURA_PLANTILLA.md` y
  `factura_hopupu.html` — cambios operativos (fuera de canon de memoria, no auditados
  en detalle: material de facturacion, no corresponde a esta funcion).
- Se rescataron de ramas huerfanas (PRs cerradas sin merge) los partes
  `SLEEP_2026-07-03.md`, `SLEEP_2026-07-04.md`, `SLEEP_2026-07-05.md` legibles
  agenticos que faltaban en esta rama — el shadowlog de esos dias ya estaba
  completo via el motor deterministico, pero los partes agenticos formateados
  no habian llegado a main. Ahora si.

No se detecto mutacion de fuentes fuera de lo permitido; no se toco nada bajo
`HOLD_CLINICO` / `clinical_guarded` / `[N2-HOLD-CLI]` / `[N2-HOLD-NEM]` /
`00_BOVEDA_NEXUS`.

## N2 — Consolidacion

- El camino agentico (Routine claude-code) estuvo 5 noches sin correr
  (07-03..07-07); solo el camino deterministico (GitHub Actions) sostuvo el
  shadowlog en ese hueco. Los dos caminos son redundantes por diseno
  (`CLAUDE.md` — "dos caminos de ejecucion"), asi que no hay perdida de
  cobertura, pero vale registrarlo: si algun dia el motor deterministico
  tambien falla, esos dias quedarian ciegos.
- Wave12 de Metatron se sello y se hiberno el 2026-07-05 con GO C0 del Capitan
  (evidencia: `state/metatron/RETOMAR.md`, `sealed: true`). Sin wave activa
  hasta Plan Wave13 explicito.
- Aduana del Mythos se ratifico como canon (commit `ebc4762`, evidencia:
  `docs/ADUANA_MYTHOS.md` — renombrado desde `..._PROPUESTA_20260705.md`, R074).
- `CABO-013` (RUTINAS.md) seguia abierto pidiendo que el prompt vivo de la Routine
  pasara `role=Groot` en vez de `role=Nami`. La invocacion de esta corrida ya
  llego con `role=Groot` explicito, asi que el fix del prompt parece aplicado en
  el formulario de la Routine (evidencia indirecta: el evento recibido, no el
  formulario en si — no puedo leer `claude.ai/code/routines` desde aqui). No
  cierro el cabo sin que el Capitan lo confirme; lo marco como
  "evidencia parcial a favor" en vez de resuelto.

## N3 — Sueno profundo (auditoria)

**Coherencia Sophia**: sin contradicciones nuevas detectadas contra
`state/deckard/01_CANON.md` ni `state/maceta_groot/RETOMAR.md`. Las 12
decisiones estables y la regla Deckard de certeza siguen intactas.

**Hallazgo principal — deriva actor/rol, diagnosticada con evidencia (no solo
sintoma):**

El ledger mostraba a `github-actions/Groot` con una racha de 11 ciclos
consecutivos (`sleep_ledger.jsonl` lineas 14-30, streak 1→11, 06-27→07-07),
marcada `[high] role_fusion_risk` en el ultimo parte deterministico
(`sleep_report_2026-07-07T05-03-40-450Z.md`). Causa raiz encontrada en el
codigo, no solo en el sintoma:

1. `state/funcion_de_sueno/funcion_de_sueno.mjs` (fase `phase4RemRoleRotation`,
   lineas 342-366) implementa el diseno **antiguo**: rota el *rol* (Groot↔Nami↔...)
   cuando `streak >= fusionThreshold`. Esto contradice el diseno **actual**
   documentado en `.claude/skills/sueno/SKILL.md`: *"El que sueña es Groot (la
   raiz, no un personaje); por eso rota el actor, no el rol."*
2. El workflow (`sueno-nocturno.yml`) tuvo brevemente un paso que calculaba el
   rol dinamicamente contra `roleLedger` para intentar disparar esa rotacion de
   rol — pero el propio comentario que traia ese paso (visto en el diff de
   `5d11ad2`) admite que la racha `github-actions/Groot` llego a x8 "sin
   dispararse nunca", es decir, el mecanismo tampoco funcionaba en la practica.
3. Hoy (`5d11ad2`, 2026-07-07 14:30, PR #41) el Capitan **elimino ese paso** y
   volvio a fijar `--role Groot` a mano en el workflow. Esto alinea el
   *disparador* deterministico con el diseno actual (rol siempre Groot). Pieza
   resuelta.
4. Lo que queda suelto: `funcion_de_sueno.mjs` sigue calculando y **reportando**
   un "rol sugerido" cada noche (linea de la fase REM del parte deterministico)
   que ya no aplica bajo el diseno actual — es ruido cosmetico, no un riesgo
   real, pero puede confundir a quien lea el parte pensando que hay una
   rotacion pendiente sin resolver. Clasifico esto `[low] mensaje_desactualizado`
   en el motor deterministico; requiere una edicion de codigo fuera del alcance
   de escritura de este skill (`funcion_de_sueno.mjs` no es uno de los tres
   archivos permitidos). Queda para quien mantenga ese motor.

**Falsos positivos de wikilinks huerfanos** (recurrentes desde 06-27, verificados
hoy con lectura directa, no solo con el detector regex del motor):

- `docs/MIGRACION_SEMANTICA.md` linea 118 — `[[N3-ACT-SIS-001]]` es un ejemplo
  ilustrativo de sintaxis dentro del propio documento que explica el formato de
  wikilinks (el ID de ejemplo aparece en frontmatter unas lineas antes, linea 105).
  No es un enlace roto real.
- `state/metatron/genoma/N0-SEMILLA-METATRON.md` linea 41 — `[[WikiLinks]]`
  es texto generico describiendo una accion futura ("Creara los [[WikiLinks]]
  para formar las raices"), no un enlace a un documento concreto.
- `.claude/skills/zoro-migrate/SKILL.md` — el motor no aisla el token exacto en
  su mensaje ("No target found for [[...]]"); no se encontro un wikilink literal
  en el archivo al inspeccionarlo. Probable falso positivo del mismo detector
  regex ingenuo.

Ninguno de los tres se cierra como resuelto sin tocar el detector — solo se
deja constancia de que, leidos a mano, no representan enlaces rotos reales.

**Marcadores pendientes** (bajo severidad, sin cambio): `bridge-linux/ARQUITECTURA.md`
(~5-8 marcadores pending/hold/validate segun el patron usado) y
`state/metatron/REEF_GROWTH_ARCHITECTURE.md` (~11). Documentos de arquitectura
en construccion activa; consistente con noches anteriores, no es deriva nueva.

**Hub / sumidero**: no accesible desde este entorno (infraestructura GitHub, sin
`$SUNNY_HUB_PATH`). Se declara explicitamente en vez de fabricar estado, segun
CLAUDE.md.

## REM — Integracion

**Riesgo de fusion actor/rol — aplica el guardrail a esta misma corrida:**

Filtrando el ledger solo por `actor=claude-code` (que es como el diseno actual
mide la racha, ignorando las corridas de `github-actions` intercaladas): Groot
en 07-01, Groot en 07-02, y Groot hoy 07-08 (la racha `Nami` de 06-30 corta la
cadena antes) → **racha = 3**, cruza `fusionThreshold=3`. Corresponde marcar
`rotate:true` y avisar.

Aviso honesto: rotar el *actor* (no el rol, que queda fijo en Groot por diseno)
significa que el proximo ciclo deberia correrlo un modelo distinto de
`claude-code` (p. ej. `codex` o `chatgpt`, como preve
`FUNCION_DE_SUENO_spec.md`). Hoy no hay ningun otro actor agentico conectado a
`/sueno` (`RUTINAS.md` solo lista Claude Routine + GitHub Actions, y
GitHub Actions es un motor deterministico de identidad fija, no un actor que
pueda "interpretar" un rol). Clasifico esto `pending-rearchitect`: la rotacion
de actor no se puede ejecutar honestamente hasta que exista un segundo actor
agentico conectado. No se finge una rotacion que no puede pasar.

**Aprendizaje del ciclo**: la deriva de rol/actor de las ultimas dos semanas no
era ruido — tenia una causa de codigo identificable (motor deterministico con
diseno desactualizado + trigger que no aplicaba su propia recomendacion). El
Capitan ya corrigio el trigger hoy. Recordar mejor, no mas: la proxima vez que
se vea una racha larga, revisar primero si el diseno cambio antes de asumir que
el guardrail esta roto.

## Siguiente accion minima segura

1. Ninguna accion destructiva ni de codigo desde este skill (fuera de alcance
   de escritura). Si se quiere limpiar el mensaje "rol sugerido" obsoleto en
   `funcion_de_sueno.mjs`, es una tarea de codigo aparte, no de esta corrida.
2. Cuando exista un segundo actor agentico real conectado a `/sueno`, usarlo
   para el proximo `daily_tick` y asi resolver la racha `claude-code/Groot=3`
   con una rotacion real, no simbolica.
3. Confirmar en `claude.ai/code/routines` que el prompt vivo ya dice
   `role=Groot` (evidencia indirecta a favor hoy) para poder cerrar `CABO-013`
   en `RUTINAS.md` con certeza, no con inferencia.

## Bitacora GAS

`BITACORA_GAS_URL` y `BITACORA_GAS_TOKEN` estaban definidos en este entorno.
POST realizado con `action=log_cowork`, `nakama=Usopp`, `tema=caso0`:
respuesta `200 {"ok":true,"timestamp":"2026-07-08T01:15:26.033Z","nakama":"Usopp"}`.
Mensaje enviado:

```
nakama=Usopp
tema=caso0
mensaje=Sueno 2026-07-08: causa raiz de la racha github-actions/Groot x11 identificada (mjs desactualizado + trigger ya corregido hoy por el Capitan en PR 41). claude-code/Groot alcanza racha=3 (fusionThreshold) sin segundo actor disponible para rotar; pending-rearchitect. Sin contradicciones nuevas de canon. 3 wikilinks "huerfanos" verificados como falsos positivos del detector.
```

## Calibracion

v1 aspiracional: este parte no garantiza ausencia total de alucinacion ni
simulacion fuerte de atractores. La causa raiz de la deriva de rol/actor se
verifico contra codigo y commits reales (no solo contra el ledger), pero sigue
siendo lectura humana asistida, no una prueba formal.
